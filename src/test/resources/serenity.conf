serenity {
  project.name = "Test E2E"
  logging = "VERBOSE"
  take.screenshots.for.tasks = "after_each_step"
  take.screenshots.for.interactions = "for_failures"
  take.screenshots.for.questions = "after_each_step"
}

#webdriver.driver=chrome
#webdriver.chrome.driver=src/test/resources/webdriver/chromedriver133.exe
#webdriver.manage.driver.auto-download=true

webdriver {
  driver = "chrome"
  chrome {
    driver = "webdriver.chrome.driver"
    driverPath = "src/test/resources/webdriver/chromedriver133.exe"
    autoDownload = true
    switches = ["--remote-allow-origins=*"]
  }
}
 webdriver {
 webdriver.driver=chrome
   capabilities {
     browserName = "Chrome"
     browserVersion = "133"
     platformName = "Windows 11"
     acceptInsecureCerts = true
      "goog:chromeOptions" {
           args = ["disable-gpu", "disable-default-apps", "disable-popup-blocking"]
     }
     timeouts {
       script = 30000
       pageLoad = 300000
       implicit = 20000
     }
   }
}